[project]
name = "osaip-agents"
version = "0.1.0"
description = "OSAIP agents package (scaffold; first real code lands in a later phase)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["osaip_agents"]
