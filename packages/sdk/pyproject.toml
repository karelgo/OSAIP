[project]
name = "osaip"
version = "0.1.0"
description = "OSAIP SDK — osaip.dataset(...), .llm(...), .agent(...), .semantic(...)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["osaip"]
