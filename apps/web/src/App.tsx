// Placeholder — the app shell (router, rail, top bar, theme) lands with the shell slice.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>OSAIP</h1>
      <p>Phase 0 scaffold is booting. The app shell arrives in a later slice.</p>
    </main>
  );
}
